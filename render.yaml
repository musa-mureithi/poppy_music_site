services:
  - type: web
    name: poppy-music-site
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
    startCommand: gunicorn your_project_name.wsgi:application
    staticPublishPath: staticfiles
